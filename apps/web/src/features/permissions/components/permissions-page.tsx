import { PageHeader } from '@/components/shared/page-header';

export function PermissionsPage(): React.JSX.Element {
  return (
    <PageHeader title="Permissions" description="Manage permission sets and access controls." />
  );
}

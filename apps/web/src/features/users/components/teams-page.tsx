import { PageHeader } from '@/components/shared/page-header';

export function TeamsPage(): React.JSX.Element {
  return (
    <PageHeader title="Teams" description="Organize users into teams and groups." />
  );
}

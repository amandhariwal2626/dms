import { PageHeader } from '@/components/shared/page-header';

export function InvitationsPage(): React.JSX.Element {
  return (
    <PageHeader title="Invitations" description="Manage and send user invitations." />
  );
}

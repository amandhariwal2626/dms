import { PageHeader } from '@/components/shared/page-header';

export function RoleAssignmentsPage(): React.JSX.Element {
  return (
    <PageHeader
      title="Role Assignments"
      description="Assign roles to users and manage their access."
    />
  );
}

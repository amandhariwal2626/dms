import { PageHeader } from '@/components/shared/page-header';

export function RolesPage(): React.JSX.Element {
  return (
    <PageHeader title="Roles" description="Define and manage system roles." />
  );
}
